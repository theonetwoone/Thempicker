# WordPress export — Diætist Danmark | Autoriseret Klinisk Diætistvejledning

## Quick paste (no theme files)

1. Edit a page in the block editor.
2. Add a **Custom HTML** block and paste `block-editor/page-content.html`.
3. Go to **Appearance → Customize → Additional CSS** and paste `block-editor/additional-css.css`.
4. If you need JavaScript, use a plugin like "Insert Headers and Footers" and paste `assets/script.js` in the footer (after jQuery if your script depends on it).

## Full theme integration (recommended)

1. Copy `theme/variated/di-tist-danmark-autoriseret-klinisk-di-tistvejledning/` into your **child theme** root (keep the `variated/di-tist-danmark-autoriseret-klinisk-di-tistvejledning` path).
2. Copy `theme/variated-di-tist-danmark-autoriseret-klinisk-di-tistvejledning-template.php` to your child theme root.
3. Add the code from `theme/functions-snippet.php` to your child theme `functions.php`.
4. Create a new page in WordPress and assign the template **"Diætist Danmark | Autoriseret Klinisk Diætistvejledning"**.

## CDN dependencies

This design may rely on external libraries loaded in the HTML head. The page template includes those tags. If you use the block-editor-only path, add the same `<link>` / `<script>` tags via your theme or a header/footer plugin.

Generated by Variated AI Designer.
